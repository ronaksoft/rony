package tools

import (
	"testing"
)

/*
   Creation Time: 2021 - Apr - 09
   Created by:  (ehsan)
   Maintainers:
      1.  Ehsan N. Moosa (E2)
   Auditor: Ehsan N. Moosa (E2)
   Copyright Ronak Software Group 2020
*/

func TestTimeUnix(t *testing.T) {
	// Convey("TimeUnix", t, func(c C) {
	// 	wg := sync.WaitGroup{}
	// 	for i := 0; i < 100; i++ {
	// 		wg.Add(1)
	// 		go func() {
	// 			oldT := TimeUnix()
	// 			for j := int64(0); j < 5; j++ {
	// 				time.Sleep(time.Second + time.Millisecond*100)
	// 				newT := TimeUnix()
	// 				c.So(newT, ShouldEqual, oldT+j+1)
	// 			}
	// 			wg.Done()
	// 		}()
	// 	}
	// 	wg.Wait()
	// })
}
