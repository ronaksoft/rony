package gogen

/*
   Creation Time: 2020 - Apr - 04
   Created by:  (ehsan)
   Maintainers:
      1.  Ehsan N. Moosa (E2)
   Auditor: Ehsan N. Moosa (E2)
   Copyright Ronak Software Group 2018
*/

// A Plugin provides functionality to add to the output during Go code generation,
// such as to produce RPC stubs.
type Plugin interface {
	// Name identifies the plugin.
	Name() string
	// Init is called once after data structures are built but before
	// code generation begins.
	Init(gen *Generator)
	// Generate produces the code generated by the plugin for this descriptor,
	// except for the imports, by calling the generator's methods P, In, and Out.
	Generate(desc *Descriptor)
	// GeneratePrepend will be called to prepend to the generated code.
	GeneratePrepend(desc *Descriptor)
}
