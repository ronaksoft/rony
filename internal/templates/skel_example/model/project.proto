syntax = "proto3";
package model;
option go_package = "<%= goPackage() %>/model";

// Project
// {{@model cql}}
// {{@tab (ID)}}
message Project {
    int64 ID = 1;
    string Title = 3;
    string Desc = 4;
    repeated int64 UserIDs = 7;
}